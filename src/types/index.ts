export interface navLinks {
  link: string;
  label: string;
}

export interface storeLinks_t {
  link: string;
  img: string;
  ariaLabel: string;
}

export interface cards_t {
  title: string;
  img: string;
  txt: string;
}
